package org.aerial_dad.alexplugin.Sumo.listeners;

import org.bukkit.event.Listener;
import org.bukkit.event.player.PlayerInteractEvent;


public class onrightclick implements Listener {

    public void onClick(PlayerInteractEvent e){

    }
}

/**
 * command that will place a block with custom data
 * on click check for the data
 * if data matches run a command that will teleport the player to the Sumo arena.
 *
 */
